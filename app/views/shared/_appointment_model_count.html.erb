<%# locals: (model:) -%>
<div>
  <p class="my-1">
    <span>Today: </span>
    <span class="font-bold"><%= pluralize(model.appointments.today.size, "appointment") %></span>
  </p>
  <p class="my-1">
    <span>Past: </span>
    <span class="font-bold"><%= pluralize(model.appointments.past.size, "appointment") %></span>
  </p>
  <p class="my-1">
    <span>Upcoming: </span>
    <span class="font-bold"><%= pluralize(model.appointments.upcoming.size, "appointment") %></span>
  </p>
  <p class="my-1">
    <span>Total: </span>
    <span class="font-bold"><%= pluralize(model.appointments.size, "appointment") %></span>
  </p>
</div>
